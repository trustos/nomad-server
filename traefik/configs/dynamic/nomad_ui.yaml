http:
  routers:
    nomad-ui:
      rule: "Host(`nomad.rs-estates`)"
      entryPoints:
        - "web"
      service: nomad-ui
      middlewares:
        - "auth"

  services:
    nomad-ui:
      loadBalancer:
        servers:
          - url: "http://nomad.service.consul:4646"
